# 40.	Write a Python program to format a number with a percentage.

def add_percentage_symbol(number):
    print(str(number) +"%")

number=int(input("\nEnter the number : "))
add_percentage_symbol(number)